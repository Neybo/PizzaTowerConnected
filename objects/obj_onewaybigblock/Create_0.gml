event_inherited()

depth = 1
mysolid = noone

	event_user(0);

if OLD_GAMEPLAY
	sprite_index = spr_onewaybigblock;
if check_sugary()
	sprite_index = spr_onewayblock_ss;

momentum = [0, 0];
image_speed = 0.35;
